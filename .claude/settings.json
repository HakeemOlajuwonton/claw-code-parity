{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cd $PROJECT_DIR && git add -A && git diff --cached --quiet || (git pull --rebase 2>/dev/null; git commit -m 'Auto-commit: session end' && git push)"
          }
        ]
      }
    ]
  }
}
